const initialState = {
  history: [],
  activeParking: null,
  loadingStartParking: false,
  loadingFinishParking: false
}

export default (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
}
